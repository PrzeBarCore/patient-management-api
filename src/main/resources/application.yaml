spring:
  application:
    name: Laboratory-management-system
  datasource:
    url: jdbc:postgresql://localhost:5432/laboratoryManagement
    username: postgres
    password: postgres

  jpa:
    Hibernate:
      ddl-auto: validate
    show-sql: true

  flyway:
    enabled: true
  server:
    port: 8080
